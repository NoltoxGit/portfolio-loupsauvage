export function App() {
  return (
    <main className="phase-one-app" aria-labelledby="phase-one-title">
      <section className="phase-one-panel">
        <p className="phase-one-kicker">Portfolio LoupSauvage</p>
        <h1 id="phase-one-title">Socle React/Vite pret</h1>
        <p>
          Cette application est volontairement isolee du site public existant.
          Le portage visuel commencera apres validation du contrat API public.
        </p>
      </section>
    </main>
  );
}
