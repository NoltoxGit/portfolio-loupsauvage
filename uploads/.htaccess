# Uploads must be served as static files only.
Options -Indexes

<FilesMatch "\.(php|phtml|phar|cgi|pl|py|sh|bash)$">
    Require all denied
</FilesMatch>

<IfModule mod_php.c>
    php_flag engine off
</IfModule>

<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
</IfModule>
